<?php
namespace Apie\StorageMetadataBuilder\Interfaces;

/**
 * This marker interface is added to make sure it 's a root entity, so the one that could be referenced from an
 * Apie domain object.
 */
interface RootObjectInterface
{
}
